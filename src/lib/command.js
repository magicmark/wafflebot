/**
 * Enum types for message command actions
 */
export default {
    JOIN: Symbol('JOIN'),
    NOTIFY: Symbol('NOTIFY'),
    FIGHT: Symbol('FIGHT'),
    MEME: Symbol('MEME'),
    DEFAULT: Symbol('DEFAULT'),
};
