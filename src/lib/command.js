/**
 * Enum type for message command actions
 *
 * @readonly
 * @enum {Symbol}
 */
export default {
    JOIN: Symbol('JOIN'),
    NOTIFY: Symbol('NOTIFY'),
    FIGHT: Symbol('FIGHT'),
    MEME: Symbol('MEME'),
    DEFAULT: Symbol('DEFAULT'),
};
